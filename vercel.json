{
  "version": 2,
  "buildCommand": "ng build --configuration production",
  "outputDirectory": "dist/energy-dashboard-frontend",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
